version: '3'
services:
  my_service:
    build:
      context: ./docker_image_creation
    image: pe_env:latest
    container_name: pe_env_container
    restart: always
    stdin_open: true
    tty: true
